
import numpy as np


class Config(object):    
    # initialize the initial learning rate, number of epochs to train for,
    # and batch size
    # learning rate
    INIT_LR = 1e-4

    # max # of epochs
    EPOCHS = 20

    # batch size
    BS = 32

